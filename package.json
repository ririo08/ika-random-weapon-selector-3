{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "yarn weapon && nuxt dev",
    "generate": "yarn weapon && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "weapon": "node parse.js"
  },
  "devDependencies": {
    "nuxt": "^3.2.2"
  },
  "dependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/papaparse": "^5.3.7",
    "axios": "^1.3.4",
    "fs-extra": "^11.1.0",
    "papaparse": "^5.4.0"
  }
}
